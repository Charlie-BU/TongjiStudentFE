# 前端本地开发端口
VITE_DEV_SERVER_PORT=5173

# 生产后端 Base URL（被 Caddy 代理至 /api）
VITE_TONGJI_STUDENT_BASE_URL=<TONGJI_STUDENT_BASE_URL>

# 浏览器直接请求的 API 基地址。生产环境使用 /api
VITE_TONGJI_STUDENT_DIRECT_BASE_URL=/api

# 是否开启测试调试入口
TEST_ENV=false
