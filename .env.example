# 前端本地开发端口
VITE_DEV_SERVER_PORT=5173

# TongjiStudent Server 服务的 Base URL；浏览器会直接访问该地址。
# 本地开发示例：http://127.0.0.1:8080
# 生产环境请填写公开的 HTTPS 服务地址，并在后端 CORS_ALLOW_ORIGINS中允许对应前端 Origin。
VITE_TONGJI_STUDENT_BASE_URL=http://127.0.0.1:8080

# 是否开启测试调试入口
TEST_ENV=false
